package ch.hftm.api.models.enums;

/**
 * The two states that a window can have.
 * This is limited by the mechanical functions of the windows.
 */
public enum StateType {

    OPEN {
    },
    CLOSED {
    };
}
